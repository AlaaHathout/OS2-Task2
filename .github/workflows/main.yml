on:
  push:
    branches:
      - master


jobs:
  build:
    runs-on: ubuntu-latest
    steps:
      - name: 'Before uses'
        run: ls

      - uses: actions/checkout@v3

      - name: 'Login to doker'
        run: docker login --username alaahathout --password dckr_pat_7iwwve_qw_y1qt8kkC-vxADdO_k
